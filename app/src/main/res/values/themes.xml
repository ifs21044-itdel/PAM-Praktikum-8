<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WhatsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/navcolor</item>
        <item name="colorPrimaryVariant">@color/navcolor</item>
        <item name="android:statusBarColor">@color/navcolor</item>
    </style>

    <style name="Theme.WhatsApp" parent="Base.Theme.WhatsApp" />

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/hovernav</item>
    </style>
</resources>